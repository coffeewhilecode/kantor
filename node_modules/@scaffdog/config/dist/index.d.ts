import { Config, ResolvedConfig } from '@scaffdog/types';

declare const validateConfig: (maybeConfig: unknown) => Config;

type LoadConfigResult = {
    filepath: string;
    config: ResolvedConfig;
};
declare const loadConfig: (project?: string) => LoadConfigResult;

export { LoadConfigResult, loadConfig, validateConfig };
