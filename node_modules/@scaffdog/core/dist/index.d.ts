import { Program, ParseOptions } from '@scaffdog/engine';

type VariableSourceMap = Map<string, Program>;
type Template = {
    filename: Program;
    content: Program;
};

type ExtractOptions = Partial<ParseOptions>;
type ExtractResult = {
    variables: VariableSourceMap;
    templates: Template[];
};
declare const extract: (input: string, options: ExtractOptions) => ExtractResult;

export { ExtractOptions, ExtractResult, Template, VariableSourceMap, extract };
